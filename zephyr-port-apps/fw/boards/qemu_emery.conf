CONFIG_INPUT=y
CONFIG_FLASH_PAGE_LAYOUT=y
# RTC_TICKS_HZ derives from this; the animation rate-control loop needs ~1ms
# resolution (shipping runs 1024 Hz) or frame cadence oscillates.
CONFIG_SYS_CLOCK_TICKS_PER_SEC=1000
