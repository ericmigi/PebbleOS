/* SPDX-License-Identifier: Apache-2.0 */

#pragma once

#define PBL_HEXDUMP_D_SERIAL(level, data, length) \
  do {                                             \
    (void)(level);                                 \
    (void)(data);                                  \
    (void)(length);                                \
  } while (0)
